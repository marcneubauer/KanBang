# Feature Ideas

1. **"Done" column** — auto-move checked/completed items to a dedicated "Done" list
2. **Board settings menu** — move the archive board button into a "..." overflow menu for board settings
3. **Board background image** — add a config option to set a background image per board
4. **Password reset flow** — "forgot password" page with email-based reset tokens (requires SMTP setup)
